package com.lufylegend.sgj;public class AliPayResult {		public String resultStatus = "";		public String result = "";	public String memo = "";	private String resultStatusMsg = "";	private boolean succeed = false;		public AliPayResult(String rawResult) {		try {			String[] resultParams = rawResult.split(";");			for (String resultParam : resultParams) {				if (resultParam.startsWith("resultStatus")) {					resultStatus = gatValue(resultParam, "resultStatus");				}				if (resultParam.startsWith("result")) {					result = gatValue(resultParam, "result");				}				if (resultParam.startsWith("memo")) {					memo = gatValue(resultParam, "memo");				}			}			// 判断resultStatus 为“9000”则代表支付成功，具体状态码代表含义可参考接口文档			// “8000” 代表支付结果因为支付渠道原因或者系统原因还在等待支付结果确认，最终交易是否成功以服务端异步通知为准（小概率状态）			if (resultStatus!=null && (resultStatus.equals("9000") || resultStatus.equals("8000"))) {				succeed = true;				resultStatusMsg = "succeed";			} else {				succeed = false;				resultStatusMsg = "failed";			}		} catch (Exception e) {			e.printStackTrace();		}	}	@Override	public String toString() {		return "resultStatus={" + resultStatus + "};memo={" + memo				+ "};result={" + result + "}";	}	private String gatValue(String content, String key) {		String prefix = key + "={";		return content.substring(content.indexOf(prefix) + prefix.length(),				content.lastIndexOf("}"));	}		public boolean isSucceed() {				return succeed;	}		public String getResultStatusMsg() {		return resultStatusMsg;	}}